{
  "name": "kinefix",
  "version": "1.0.0",
  "description": "Software de gestión para gimnasios: socios, rutinas, cuotas y progreso.",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix fitpulse-pro install",
    "seed": "npm --prefix server run seed",
    "dev": "concurrently -n api,web -c cyan,green \"npm --prefix server run dev\" \"npm --prefix fitpulse-pro run dev\"",
    "build": "npm --prefix fitpulse-pro run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix server test",
    "lint": "npm --prefix fitpulse-pro run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
